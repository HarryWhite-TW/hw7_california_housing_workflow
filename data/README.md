# Data

The main workflow expects:

```text
data/housing.csv
```

If the file is missing, `configs/california_housing.json` instructs the workflow to download it from the teacher-provided public source.

The CSV is intentionally ignored by Git so the repository remains lightweight and the source remains explicit.
